# Changelog

## 0.2.0 - 2026-08-21

- Package Project Flood as an Agent Plugins 1.0 plugin and Copilot marketplace entry.
- Add full and plugin-adapter installation modes with baseline-hash manifests.
- Namespace nine agents and ten portable skills; add Flood Integrator and proportional specification workflow.
- Replace critical prompt-only workflows with skills while retaining local prompt wrappers.
- Add deterministic hook policy, task ownership manifests, hashed session keys, and metadata-only audit.
- Adopt canonical Markdown memory with YAML frontmatter, generated YAML index, and ephemeral runtime JSON.
- Add cloud-agent setup, harness matrix, CODEOWNERS examples, threat model, adversarial eval cases, and pinned cross-platform CI.
- Add `validate`, `memory-index`, `task-validate`, `doctor`, `diff`, `install`, `upgrade`, `migrate`, and recoverable `uninstall` commands.

## 0.1.0 - 2026-08-20

- Initial repository-native squad/swarm template with eight agents, six skills, safe copy installers, and structural validation.
